repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        exclude: ^charts/.*/templates/.*\.yaml$
      - id: check-added-large-files
      - id: check-merge-conflict

  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: cargo fmt --all --check
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-clippy
        name: cargo clippy
        entry: env K8S_OPENAPI_ENABLED_VERSION=1.30 cargo clippy --workspace --all-targets --all-features -- -D warnings
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-test
        name: cargo test
        entry: env K8S_OPENAPI_ENABLED_VERSION=1.30 cargo test --workspace --all-features
        language: system
        types: [rust]
        pass_filenames: false
        stages: [pre-push]

      - id: check-unsafe-shell
        name: check unsafe shell patterns
        entry: bash scripts/check-unsafe-shell.sh
        language: system
        types: [shell]
        pass_filenames: false

      - id: schema-validate
        name: schema validate examples/config-samples
        entry: cargo run --quiet --bin schema-validate --
        language: system
        files: >-
          (?x)^(
            examples/.*\.ya?ml|
            config/samples/.*\.ya?ml|
            config/crd/.*\.ya?ml|
            \.schema-validate-ignore|
            src/bin/schema_validate\.rs
          )$
        pass_filenames: false

      - id: crd-drift-check
        name: crd drift check (config/crd vs helm chart)
        entry: cargo run --quiet --bin crd-drift-check --
        language: system
        files: >-
          (?x)^(
            config/crd/.*\.ya?ml|
            charts/stellar-operator/templates/.*crd.*\.ya?ml|
            \.crd-drift-baseline\.toml|
            src/bin/crd_drift_check\.rs
          )$
        pass_filenames: false
        args: ["--warn-only"]

      - id: check-stale-docs
        name: check stale docs
        entry: cargo run --quiet --bin doc-check --
        language: system
        # Run whenever Rust source, docs, or the coverage config changes.
        files: >-
          (?x)^(
            src/.*\.rs|
            docs/.*\.md|
            doc-coverage\.toml|
            \.doc-hashes\.toml|
            Makefile|
            Cargo\.toml|
            build\.rs|
            charts/.*|
            examples/.*|
            config/.*|
            benchmarks/.*|
            formal_verification/.*|
            tests/.*\.rs
          )$
        pass_filenames: false
        # Only warn locally — CI enforces the hard failure.
        args: ["--warn-only", "status"]

  - repo: https://github.com/adrienverge/yamllint.git
    rev: v1.35.1
    hooks:
      - id: yamllint
        args: [-c=.yamllint.yml]
        exclude: ^charts/.*/templates/.*\.yaml$
