# secret-vault.yaml — example Secret reference for validator seed (dev only)
apiVersion: v1
kind: Secret
metadata:
  name: validator-seed-local
  namespace: stellar
type: Opaque
stringData:
  seed: "REPLACE_WITH_TESTNET_SEED"
