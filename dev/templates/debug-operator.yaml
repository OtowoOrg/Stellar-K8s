# debug-operator.yaml — dev overlay with verbose operator logging
apiVersion: stellar.org/v1alpha1
kind: StellarNode
metadata:
  name: debug-validator
  namespace: stellar
spec:
  nodeType: Validator
  network: testnet
  version: "v21.0.0"
  maxUnavailable: 0
  minAvailable: 1
  topologySpreadConstraints: []
