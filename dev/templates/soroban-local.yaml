# soroban-local.yaml — Soroban RPC dev overlay (extends _base-stellarnode.yaml)
apiVersion: stellar.org/v1alpha1
kind: StellarNode
metadata:
  name: soroban-local
  namespace: stellar
spec:
  nodeType: SorobanRpc
  network: testnet
  version: "21.0.0"
  sorobanConfig:
    stellarCoreUrl: "http://validator-local:11626"
