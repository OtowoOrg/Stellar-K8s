# validator-local.yaml — local validator overlay (extends _base-stellarnode.yaml)
apiVersion: stellar.org/v1alpha1
kind: StellarNode
metadata:
  name: validator-local
  namespace: stellar
spec:
  nodeType: Validator
  network: testnet
  validatorConfig:
    seedSecretRef: "validator-seed-local"
