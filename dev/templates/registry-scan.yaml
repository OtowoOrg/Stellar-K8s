# registry-scan.yaml — dev StellarNode with pinned image for registry validation
apiVersion: stellar.org/v1alpha1
kind: StellarNode
metadata:
  name: registry-scan
  namespace: stellar
spec:
  nodeType: Validator
  network: testnet
  version: "v21.0.0"
  image: "stellar/stellar-core:21.0.0"
